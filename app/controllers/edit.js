import Ember from 'ember';

export default Ember.Controller.extend({
	//actions moved to route
	//actions: {
		// updatePost(post){
		// 	console.log('debugging create',arguments);
		// 	 post.save().then(result => {
		// 		this.transitionToRoute('post',post.id); 	
		// 	 });
		// },
		// goToFullView(id){
		// 	this.transitionToRoute('post',id);
		// }
	//}
});
