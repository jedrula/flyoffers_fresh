import { cloudinaryTag } from '../../../helpers/cloudinary-tag';
import { module, test } from 'qunit';

module('Unit | Helper | cloudinary tag');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = cloudinaryTag(42);
  assert.ok(result);
});
